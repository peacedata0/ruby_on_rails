// Powered by Infostretch 

timestamps {

node () {

	stage ('helloworld - Checkout') {
 	 checkout([$class: 'GitSCM', branches: [[name: '*/master']], doGenerateSubmoduleConfigurations: false, extensions: [], submoduleCfg: [], userRemoteConfigs: [[credentialsId: 'cde2d286-4b1b-4b88-a424-6a0fd647b6cd', url: 'https://github.com/peacedata0/ruby_on_rails']]]) 
	}
// Unable to convert a build step referring to "ruby-proxy-object". Please verify and convert manually if required.
// Unable to convert a build step referring to "ruby-proxy-object". Please verify and convert manually if required.
}
}