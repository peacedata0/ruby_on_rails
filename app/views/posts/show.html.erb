<h1><%= @post.title %></h1>

<div class="d-inline p-2 bg-dark text-white">
<%= link_to 'Изменить', edit_post_path(@post) %>
<%= link_to 'Удалить', post_path(@post), method: :delete, data: {confirm: 'Не, серьезно?'} %>
</div>

<p><%= @post.body %></p>